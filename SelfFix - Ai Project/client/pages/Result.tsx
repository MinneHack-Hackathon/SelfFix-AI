import { useNavigate } from "react-router-dom";
import { useEffect, useState } from "react";
import { Download, ChevronDown, ChevronUp, AlertCircle, Zap } from "lucide-react";

interface DiagnosisResult {
  diagnosis: {
    most_likely_issue: string;
    confidence_percent: number;
    severity: string;
  };
  cost_analysis: {
    estimated_repair_cost: number;
    estimated_replacement_cost: number;
    estimated_savings: number;
  };
  environmental_impact: {
    carbon_saved_kg: number;
  };
  repair_details: {
    difficulty_level: string;
    tools_required: string[];
    steps: string[];
    safety_warning: string;
  };
}

export default function Result() {
  const navigate = useNavigate();
  const [result, setResult] = useState<DiagnosisResult | null>(null);
  const [expandedSection, setExpandedSection] = useState<string | null>(
    "repair"
  );

  useEffect(() => {
    const storedResult = localStorage.getItem("diagnosisResult");
    if (storedResult) {
      setResult(JSON.parse(storedResult));
    } else {
      navigate("/diagnose");
    }
  }, [navigate]);

  const getConfidenceColor = (confidence: number) => {
    if (confidence >= 80) return "text-green-600";
    if (confidence >= 60) return "text-yellow-600";
    return "text-orange-600";
  };

  const getDifficultyColor = (difficulty: string) => {
    switch (difficulty) {
      case "Easy":
        return "text-green-600 bg-green-50";
      case "Moderate":
        return "text-yellow-600 bg-yellow-50";
      case "Hard":
        return "text-red-600 bg-red-50";
      default:
        return "text-slate-600 bg-slate-50";
    }
  };

  const handleDownloadReport = () => {
    if (!result) return;

    const reportContent = `
DIAGNOSIS REPORT - SelfFix AI
========================================

DIAGNOSIS RESULT
Most Likely Issue: ${result.diagnosis.most_likely_issue}
Confidence: ${result.diagnosis.confidence_percent}%
Severity: ${result.diagnosis.severity}

COST ANALYSIS
Estimated Repair Cost: $${result.cost_analysis.estimated_repair_cost}
Estimated Replacement Cost: $${result.cost_analysis.estimated_replacement_cost}
Estimated Savings: $${result.cost_analysis.estimated_savings}

ENVIRONMENTAL IMPACT
Carbon Saved: ${result.environmental_impact.carbon_saved_kg} kg CO₂

DIY DIFFICULTY
Difficulty Level: ${result.repair_details.difficulty_level}

TOOLS REQUIRED
${result.repair_details.tools_required.map((tool) => `- ${tool}`).join("\n")}

REPAIR STEPS
${result.repair_details.steps
  .map((step, i) => `${i + 1}. ${step}`)
  .join("\n")}

SAFETY WARNING
⚠ ${result.repair_details.safety_warning}

Generated by SelfFix AI
    `.trim();

    const element = document.createElement("a");
    element.setAttribute(
      "href",
      "data:text/plain;charset=utf-8," + encodeURIComponent(reportContent)
    );
    element.setAttribute("download", "diagnosis_report.txt");
    element.style.display = "none";
    document.body.appendChild(element);
    element.click();
    document.body.removeChild(element);
  };

  if (!result) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-50 via-white to-blue-50">
        <div className="text-center">
          <svg className="animate-spin h-12 w-12 text-green-500 mx-auto mb-4" viewBox="0 0 24 24">
            <circle
              className="opacity-25"
              cx="12"
              cy="12"
              r="10"
              stroke="currentColor"
              strokeWidth="4"
              fill="none"
            />
            <path
              className="opacity-75"
              fill="currentColor"
              d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
            />
          </svg>
          <p className="text-slate-600">Loading results...</p>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-white to-blue-50">
      {/* Header */}
      <header className="border-b border-slate-200/50 bg-white/80 backdrop-blur-sm sticky top-0 z-50">
        <div className="max-w-2xl mx-auto px-6 py-4 flex items-center justify-between">
          <button
            onClick={() => navigate("/diagnose")}
            className="flex items-center gap-2 text-slate-600 hover:text-slate-900 font-medium"
          >
            <span>← Back</span>
          </button>
          <div className="flex items-center gap-2">
            <div className="w-8 h-8 rounded-lg bg-gradient-to-br from-green-500 to-blue-600 flex items-center justify-center">
              <Zap className="w-5 h-5 text-white" />
            </div>
            <span className="font-semibold text-slate-900">SelfFix AI</span>
          </div>
          <div className="w-24"></div>
        </div>
      </header>

      {/* Main Content */}
      <main className="max-w-2xl mx-auto px-6 py-12">
        <div className="mb-8">
          <h1 className="text-4xl font-bold text-slate-900 mb-2">
            Diagnosis Result
          </h1>
          <p className="text-slate-600">
            Here's what we found and how to fix it.
          </p>
        </div>

        <div className="space-y-6">
          {/* Diagnosis Card */}
          <div className="bg-white rounded-xl border border-slate-200 shadow-sm p-8">
            <h2 className="text-lg font-semibold text-slate-900 mb-4">
              Most Likely Issue
            </h2>
            <div className="mb-4">
              <p className="text-2xl font-bold text-slate-900">
                {result.diagnosis.most_likely_issue}
              </p>
            </div>
            <div className="grid grid-cols-2 gap-4">
              <div>
                <p className="text-sm text-slate-600 mb-1">Confidence</p>
                <div className="flex items-center gap-2">
                  <span
                    className={`text-2xl font-bold ${getConfidenceColor(
                      result.diagnosis.confidence_percent
                    )}`}
                  >
                    {result.diagnosis.confidence_percent}%
                  </span>
                  <div className="w-32 h-2 bg-slate-200 rounded-full overflow-hidden">
                    <div
                      className="h-full bg-green-500 rounded-full"
                      style={{
                        width: `${result.diagnosis.confidence_percent}%`,
                      }}
                    ></div>
                  </div>
                </div>
              </div>
              <div>
                <p className="text-sm text-slate-600 mb-1">Severity</p>
                <p className="text-lg font-semibold text-red-600">
                  {result.diagnosis.severity}
                </p>
              </div>
            </div>
          </div>

          {/* Cost Analysis Card */}
          <div className="bg-gradient-to-br from-green-50 to-blue-50 rounded-xl border border-green-200 shadow-sm p-8">
            <h2 className="text-lg font-semibold text-slate-900 mb-6">
              Cost Analysis
            </h2>
            <div className="grid md:grid-cols-3 gap-4">
              <div className="bg-white rounded-lg p-4 border border-slate-200">
                <p className="text-sm text-slate-600 mb-2">DIY Repair</p>
                <p className="text-3xl font-bold text-blue-600">
                  ${result.cost_analysis.estimated_repair_cost}
                </p>
              </div>
              <div className="bg-white rounded-lg p-4 border border-slate-200">
                <p className="text-sm text-slate-600 mb-2">Full Replacement</p>
                <p className="text-3xl font-bold text-slate-600">
                  ${result.cost_analysis.estimated_replacement_cost}
                </p>
              </div>
              <div className="bg-white rounded-lg p-4 border border-green-200 bg-green-50">
                <p className="text-sm text-green-700 font-semibold mb-2">
                  You Save
                </p>
                <p className="text-3xl font-bold text-green-600">
                  ${result.cost_analysis.estimated_savings}
                </p>
              </div>
            </div>
          </div>

          {/* Environmental Impact Card */}
          <div className="bg-white rounded-xl border border-slate-200 shadow-sm p-8">
            <h2 className="text-lg font-semibold text-slate-900 mb-4">
              Environmental Impact
            </h2>
            <div className="flex items-center gap-4">
              <div className="w-16 h-16 rounded-lg bg-green-100 flex items-center justify-center">
                <span className="text-2xl">🌱</span>
              </div>
              <div>
                <p className="text-sm text-slate-600 mb-1">Carbon Saved</p>
                <p className="text-3xl font-bold text-green-600">
                  {result.environmental_impact.carbon_saved_kg} kg CO₂
                </p>
                <p className="text-xs text-slate-500 mt-1">
                  by repairing instead of replacing
                </p>
              </div>
            </div>
          </div>

          {/* Difficulty & Safety Card */}
          <div className="bg-white rounded-xl border border-slate-200 shadow-sm p-8">
            <h2 className="text-lg font-semibold text-slate-900 mb-4">
              DIY Assessment
            </h2>
            <div className="grid md:grid-cols-2 gap-4">
              <div className={`p-4 rounded-lg ${getDifficultyColor(result.repair_details.difficulty_level)}`}>
                <p className="text-sm font-semibold mb-1">Difficulty Level</p>
                <p className="text-xl font-bold">
                  {result.repair_details.difficulty_level}
                </p>
              </div>
              <div className="p-4 rounded-lg bg-yellow-50 border border-yellow-200">
                <p className="text-sm font-semibold text-yellow-900 mb-1">
                  Safety Notice
                </p>
                <p className="text-yellow-800 text-sm">
                  🟡 DIY With Caution
                </p>
              </div>
            </div>
          </div>

          {/* Repair Steps Section */}
          <div className="bg-white rounded-xl border border-slate-200 shadow-sm p-8">
            <button
              onClick={() =>
                setExpandedSection(
                  expandedSection === "repair" ? null : "repair"
                )
              }
              className="w-full flex items-center justify-between mb-0 hover:text-green-600 transition-colors"
            >
              <h2 className="text-lg font-semibold text-slate-900">
                Tools & Repair Steps
              </h2>
              {expandedSection === "repair" ? (
                <ChevronUp className="w-5 h-5" />
              ) : (
                <ChevronDown className="w-5 h-5" />
              )}
            </button>

            {expandedSection === "repair" && (
              <div className="mt-6 space-y-6 border-t border-slate-200 pt-6">
                {/* Tools Required */}
                <div>
                  <h3 className="font-semibold text-slate-900 mb-3">
                    Tools Required:
                  </h3>
                  <div className="space-y-2">
                    {result.repair_details.tools_required.map((tool) => (
                      <div
                        key={tool}
                        className="flex items-center gap-3 text-slate-700"
                      >
                        <div className="w-2 h-2 rounded-full bg-slate-400"></div>
                        <span>{tool}</span>
                      </div>
                    ))}
                  </div>
                </div>

                {/* Steps */}
                <div>
                  <h3 className="font-semibold text-slate-900 mb-3">Steps:</h3>
                  <div className="space-y-3">
                    {result.repair_details.steps.map((step, index) => (
                      <div key={index} className="flex gap-4">
                        <div className="flex-shrink-0 w-8 h-8 rounded-full bg-green-100 flex items-center justify-center">
                          <span className="text-sm font-semibold text-green-700">
                            {index + 1}
                          </span>
                        </div>
                        <p className="text-slate-700 pt-1">{step}</p>
                      </div>
                    ))}
                  </div>
                </div>

                {/* Safety Warning */}
                <div className="bg-yellow-50 border border-yellow-200 rounded-lg p-4 flex gap-3">
                  <AlertCircle className="w-5 h-5 text-yellow-600 flex-shrink-0 mt-0.5" />
                  <div>
                    <p className="font-semibold text-yellow-900 text-sm mb-1">
                      Safety Warning
                    </p>
                    <p className="text-yellow-800 text-sm">
                      {result.repair_details.safety_warning}
                    </p>
                  </div>
                </div>
              </div>
            )}
          </div>

          {/* Action Buttons */}
          <div className="flex gap-4">
            <button
              onClick={handleDownloadReport}
              className="flex-1 px-6 py-3 bg-slate-100 hover:bg-slate-200 text-slate-900 font-semibold rounded-xl transition-colors flex items-center justify-center gap-2"
            >
              <Download className="w-5 h-5" />
              Download Report
            </button>
            <button
              onClick={() => navigate("/diagnose")}
              className="flex-1 px-6 py-3 bg-green-500 hover:bg-green-600 text-white font-semibold rounded-xl transition-colors"
            >
              New Diagnosis
            </button>
          </div>
        </div>
      </main>
    </div>
  );
}
